# Production Environment for Render
# This file is NOT committed to git (frontend/.gitignore should exclude .env.*.local)

# Backend API URL - Will be set by Render environment variable
# Render will inject VITE_BACKEND_URL at build time
VITE_BACKEND_URL=https://pragyan-ai-nmeu.onrender.com:5000
