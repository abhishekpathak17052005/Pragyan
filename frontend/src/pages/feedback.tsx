// src/pages/feedback.tsx
// This page is no longer rendered — Feedback is now in Settings → Feedback & Support.
// The /feedback route redirects to /settings?tab=feedback (see App.tsx).
// This file is kept as an empty shell to satisfy any stale imports.

export default function FeedbackPage() {
  return null;
}
