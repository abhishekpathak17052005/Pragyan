# ─── Server ────────────────────────────────────────────
PORT=5000
NODE_ENV=development
# NODE_ENV=production   ← uncomment for production

# ─── Database (MongoDB + Prisma) ───────────────────────
# Atlas (cloud / production):
DATABASE_URL="mongodb+srv://YOUR_ATLAS_USER:YOUR_ATLAS_PASSWORD@atlas-cluster.mongodb.net/Pragyan?retryWrites=true&w=majority"

# ─── JWT ───────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="change_me_in_production_use_64_char_random_string"
JWT_EXPIRY="7d"
JWT_REFRESH_SECRET="change_me_different_64_char_random_string"
JWT_REFRESH_EXPIRY="30d"
SESSION_SECRET="change_me_session_secret"

# ─── CORS ──────────────────────────────────────────────
# Local:
FRONTEND_URL="http://localhost:5173"
CORS_ORIGINS="http://localhost:5173"
OAUTH_SUCCESS_URL="http://localhost:5173/auth/success"
OAUTH_FAILURE_URL="http://localhost:5173/auth"
# Production (comma-separate multiple origins):
# FRONTEND_URL="https://your-app.vercel.app"
RAPID_API_KEY=your api key here

# ─── OAuth Providers ─────────────────────────────────────
GOOGLE_CLIENT_ID="your_google_client_id"
GOOGLE_CLIENT_SECRET="your_google_client_secret"
GITHUB_CLIENT_ID="your_github_client_id"
GITHUB_CLIENT_SECRET="your_github_client_secret"

# ─── Bcrypt ────────────────────────────────────────────
BCRYPT_ROUNDS=10
# Increase to 12 in production for stronger hashing

# ─── RapidAPI ──────────────────────────────────────────
RAPID_API_KEY=your api key here

# ─── API Base ──────────────────────────────────────────
API_BASE_URL="http://localhost:5000"
# API_BASE_URL="https://your-backend.onrender.com"   ← production

# ─── AI Provider ──────────────────────────────────────
# Supported providers: gemini, groq, local
AI_PROVIDER=gemini
# Google Gemini API key
GEMINI_API_KEY=ypour api key here
GEMINI_MODEL=gemini-1.5-flash
# Optional model override
GEMINI_MODEL=gemini-2.5-flash

# Groq backup provider (used when Gemini fails or is disabled)
GROQ_API_KEY=your api key here
GROQ_MODEL=llama-3.1-70b-versatile

# ─── Redis Cache (optional) ───────────────────────────
# If set, explanation caching uses Redis instead of in-memory fallback
REDIS_URL=redis://127.0.0.1:6379
